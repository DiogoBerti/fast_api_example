# Exemplo de app simples em FastApi
Um pequeno exemplo de Fast Api para usar como referência

# Utilização:
Clone o repositório e, com o docker e docker-compose instalado, digite o comando "docker-compose up --build
